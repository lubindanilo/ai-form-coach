services:
  mongo:
    image: mongo:7
    container_name: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  detection:
    build:
      context: ../services/detection
    container_name: detection
    environment:
      - DATASET_CSV_PATH=/app/data/datasets/pose_samples.csv
      - POSE_DEBUG=1
      - LOG_LEVEL=INFO
    volumes:
      - ../data/datasets:/app/data/datasets
    ports:
      - "8000:8000"

  scoring:
    build:
      context: ../services/scoring
    container_name: scoring
    ports:
      - "8001:8000"

  api:
    build:
      context: ../apps/api
    container_name: api
    depends_on:
      - mongo
      - detection
      - scoring
    env_file:
      - ../.env
    ports:
      - "3000:3000"

volumes:
  mongo_data: